import React from 'react';
import { createPages } from 'gatsby';
import Layout from "../components/layout"

const Blog = () => {
  return (
    <layout>
      Hello from Blog
    </layout>
  )
}

export default Blog;
